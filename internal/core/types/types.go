package types

import "veo/pkg/types"

// Redirecting internal types to public pkg/types for compatibility
// New code should import "veo/pkg/types" directly.

type HTTPResponse = types.HTTPResponse
type FingerprintMatch = types.FingerprintMatch
type FilterResult = types.FilterResult
type PageHash = types.PageHash
type OpenPortResult = types.OpenPortResult
